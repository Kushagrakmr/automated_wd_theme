The gulp folder conatins the task which are implemented in gulp. The tasks are subdivided into two files 'styles' and 'watch' which are used to 
manage task related to the CSS and WATCH. Varioius npm packages are used and they are used tp add the functionality to the css files which allow 
us to use variables, nested CSS and mixins.