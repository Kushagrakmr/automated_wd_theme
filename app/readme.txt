This folder contains the main content of the website. The assets folder conatins the folder which contains the file related to them and then they
are used by gulp to create a temp files from those code which is served to the website.


It also contains the main website HTML file(index.html) file on which the CSS will be applied. 

